#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")

import ru.poidem.convert.AbstractStringEnumConverter;

/**
 * ${GIT_BRANCH} ${DATE}
 * @author ${USER}
 * Конвертер для {@link ${NAME}}
 */
public class ${NAME}Converter extends AbstractStringEnumConverter<${NAME}> {
    public ${NAME}Converter() {
        super(${NAME}.class);
    }
}
