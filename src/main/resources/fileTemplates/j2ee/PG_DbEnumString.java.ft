#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")

import lombok.AllArgsConstructor;
import lombok.Getter;
import ru.poidem.enums.StringEnum;

/**
 * ${GIT_BRANCH} ${DATE}
 * @author ${USER}
 * ${COMMENT}
 */
@Getter
@AllArgsConstructor
public enum ${NAME} implements StringEnum {
    
    private String val;
}
