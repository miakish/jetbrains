#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")

import ru.poidem.convert.AbstractIntegerEnumConverter;

/**
 * ${JIRA} ${DATE}
 * @author ${USER}
 * Конвертер для {@link ${NAME}}
 */
public class ${NAME}Converter extends AbstractIntegerEnumConverter<${NAME}> {
    public ${NAME}Converter() {
        super(${NAME}.class);
    }
}
